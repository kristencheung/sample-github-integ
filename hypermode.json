{
    "modelSpecs": [
        {
            "name": "distilbert-base-uncased",
            "type": "classification",
            "sourceModel": "distilbert/distilbert-base-uncased",
            "provider": "hugging-face",
            "host": "hypermode"
        }
    ],
    "embeddingSpecs": [
        {
            "entity-type": "Company",
            "attribute": "embedding",
            "modelName": "embedding-spec-1",
            "config": {
                "query": "{ about sector industry }",
                "template": "{{sector}} company in the {{industry}} industry, {{about}}"
            }
        }
    ],
    "trainingInstructions": [
        {
            "modelName": "distilbert-base-uncased",
            "labels": [
                "spams",
                "not spam"
            ]
        }
    ]
}
